info:
  name: GET_404
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/greeting?id=00000000"
  params:
    - name: id
      value: "00000000"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
