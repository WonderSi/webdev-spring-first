info:
  name: GET_path
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/greeting/{{user_id}}"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Статус должне быть 200", function() {
          expect(res.status).to.equal(200);
        });

        test("Возвращены данные пользователя", function() {
          const data = res.body;
          expect(data).to.have.property("name");
          expect(data).to.have.property("surname");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
